function gatsumOfAnArray(numbers){
    for(let i=0; i<numbers.length; i++){
        const indexx = i;
        const element = numbers[indexx]
        console.log(indexx,element)
    }
}
const myNumbers = [12, 65, 45, 78, 32, 45, 91]
gatsumOfAnArray( myNumbers)
const bigNumber = Math.max(12, 45, 65, 456, 320)
console.log(bigNumber)

const bigNumber2 = Math.min( 9, 2, 6, 22, 30, 101, -4, 30)
console.log(bigNumber2)

// Homework:1
// write a function that will take 3 numbers will return the max number.
// write a function that wil take 3 parameters and will return the min number.

// first time do it using if-else.
// second time do it using Math.min or Math.max 